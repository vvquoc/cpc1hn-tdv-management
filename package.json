{
  "name": "cpc1hn-tdv-management",
  "version": "0.1.0",
  "private": true,
  "description": "Core MVP for CPC1 Ha Noi medical representative management.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npx netlify dev",
    "check": "node scripts/build.mjs --check",
    "uat": "node scripts/uat-check.mjs && node scripts/backend-uat.mjs"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "@netlify/blobs": "^11.0.1",
    "@netlify/database": "^2.0.0",
    "pg": "^8.23.0"
  }
}
