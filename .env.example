AUTH_TOKEN_SECRET=replace-with-a-random-string-at-least-32-characters
# Chỉ dùng khi không sử dụng Netlify Database managed:
# CPC1_DATABASE_URL=postgresql://user:password@host/database?sslmode=require
